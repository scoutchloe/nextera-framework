<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nextera.order.mapper.OrderItemMapper">

    <!-- 订单明细结果映射 -->
    <resultMap id="BaseResultMap" type="com.nextera.order.entity.OrderItem">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="order_id" jdbcType="BIGINT" property="orderId"/>
        <result column="user_id" jdbcType="BIGINT" property="userId"/>
        <result column="product_id" jdbcType="BIGINT" property="productId"/>
        <result column="product_name" jdbcType="VARCHAR" property="productName"/>
        <result column="product_price" jdbcType="DECIMAL" property="productPrice"/>
        <result column="quantity" jdbcType="INTEGER" property="quantity"/>
        <result column="total_price" jdbcType="DECIMAL" property="totalPrice"/>
        <result column="created_time" jdbcType="TIMESTAMP" property="createdTime"/>
    </resultMap>

    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, order_id, user_id, product_id, product_name, product_price, quantity, total_price, created_time
    </sql>

    <!-- 根据订单ID查询订单明细列表 -->
    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order_item
        WHERE order_id = #{orderId}
        ORDER BY id
    </select>

    <!-- 批量插入订单明细 -->
    <insert id="batchInsert">
        INSERT INTO t_order_item (id, order_id, user_id, product_id, product_name, product_price, quantity, total_price, created_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.orderId}, #{item.userId}, #{item.productId}, #{item.productName}, 
             #{item.productPrice}, #{item.quantity}, #{item.totalPrice}, #{item.createdTime})
        </foreach>
    </insert>

    <!-- 根据订单ID删除订单明细 -->
    <delete id="deleteByOrderId">
        DELETE FROM t_order_item WHERE order_id = #{orderId}
    </delete>

</mapper> 